substitutions:
  _REGION: me-central1
  _SERVICE_NAME: django-hostingtest
  _IMAGE: $_REGION-docker.pkg.dev/$PROJECT_ID/$_SERVICE_NAME/app:$COMMIT_SHA

steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '$_IMAGE', '.']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', '$_IMAGE']

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      [
        'run','deploy','$_SERVICE_NAME',
        '--image','$_IMAGE',
        '--region','$_REGION',
        '--platform','managed',
        '--allow-unauthenticated',
        '--port','8080',
        '--set-env-vars','DJANGO_SETTINGS_MODULE=hostingtest.settings,DEBUG=False'
      ]

images:
  - '$_IMAGE'
